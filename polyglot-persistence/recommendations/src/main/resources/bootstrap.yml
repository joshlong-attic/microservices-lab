spring:
  application:
    name: recommendations-service
eureka:
  client:
    serviceUrl:
      defaultZone: ${vcap.services.eureka-service.credentials.uri:http://127.0.0.1:8761}/eureka/
  instance:
      hostname: ms-recommendations-service.cfapps.io
      nonSecurePort: 80